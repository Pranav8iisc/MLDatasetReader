#1. Use XML file based input. Avoids recompilation of the program if input arguments are changed.

#2. Add doxygen documentation.

#3. Add dependencies section in README
	#3.1 jpeglib.h

#4. Is there a way to define type of a data member in the class itself?
